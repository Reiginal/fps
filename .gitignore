# Dependencies
node_modules/
__pycache__/
*.pyc
venv/
.venv/

# Build output
dist/
build/

# 音を確かめるために書き出したwav。npm run sounds でいつでも作り直せるし、
# ゲーム本体は音を手続き的に合成するので実行時には要らない
sounds/

# Env / secrets
.env
.env.local
*.key

# Claude向けの手元メモ。このrepoだけの事情（起動の仕方・本番の触り方・
# 過去に外した所）を書いてある。公開リポジトリに置く物ではないので手元だけ
CLAUDE.local.md

# 手を付けていない物と、その時点の実測値を置いた手元のメモ。次に着手する時の
# 下敷きであって、遊ぶ人にもコードを読む人にも要らないので手元だけ
課題.md

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Logs
*.log

# 本物のブラウザで動かす検査の出力。落ちた時の画像と記録が入る
test-results/
playwright-report/
