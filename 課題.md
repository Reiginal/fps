# 課題

手を付けていない物と、その時点で分かっている事実を置く。
数字は `npm run sounds` / `npm run check` で測った実測値。
着手する時に測り直すところから始めなくて済むように、根拠を一緒に残す。

**番号は「1から3まで消しておいて」と言えるように振ってある。**
消したら残りの番号を振り直すこと（番号が飛んでいると、次に指す時にまた数え直しになる）。

| # | 課題 | 今の状態 |
|---|---|---|
| 1 | ナイフの当たり判定が障害物に効かない | 未着手。実測では否定できなかった仮説あり（下の節を参照） |

（2026-08-06に「足音が微妙」「1人用の敵が新しい要素を使わない」「構えの姿勢が武器ごとに違わない」を消した）
（2026-08-06に「手榴弾が長押しで投げられない」「1人プレイで発言が無反応」
「遊び中にスクショが撮れない」「設定の上下反転が要らない」を直して消した）
（2026-08-07に「銃のモデルの形がまだおかしい」を消した。ak47へ差し替えたが
弾倉の張り出しが太らせ処理と噛み合わず、結局コード製の銃に戻した。詳しくは
`assets/models/CREDITS.md`）
（2026-08-07に「はしごを登れない」を消した。**装飾のままでよいと判断**したので登る仕組みは作らず、
代わりに当たり判定が無くて体がすり抜けられたのを直した。同じ日に見つかった「板打ちの窓も
見た目は塞がっているのに弾も体もすり抜ける」（issue #57）も、窓を塞ぐ判断で一緒に直した）
（2026-08-09に「手榴弾の軌道が途中で見えなくなる」「買ったモデルだと手が浮いて見える」
「すり抜けられる物がある」を消した。**どれも直していない。** 軌道は誤報を潰した後
1回も再現しておらず、次に出た時に理由が画面へ出る見張りだけコードに残っている。
手が浮くのは `assets/models/rifle.glb` を置いた時の話で、そのファイルは今は無い
（外部モデルを入れ直す時の知見は issue #56）。すり抜けは「忘れていい」と言われた分。
同じ日に issue #55（ナイフの調査）も閉じた。課題.md #1と同じ話が2箇所にあったため）

---

## 1. ナイフの当たり判定が障害物に効かない（2026-08-06、追記あり）

遊んでいて「ナイフ振った時の当たり判定無くなってない？障害物に対して」と言われた。

振っても壁や物に当たらない（＝刃が当たった音も火花も出ない）。
`_resolveShot` の近接の道筋と、`stab()` を呼ぶ条件を見る所から。

**いつから壊れたかは未確認。** 2026-08-06に武器まわりを何度も触っているので、
その前後の差分を先に見ると早いかもしれない。

### 追記（2026-08-06）: 「射程が足りない」説は実測で否定した

まず疑ったのは「プレイヤーが壁際まで寄れる距離(衝突半径0.34m)に対して、
ナイフの射程(1.8m)がそもそも足りない箇所があるのでは」という仮説。

実際のlevel.js地形・実際のPlayerクラス(衝突込み)・実際のweapons.jsの
射程値を使い、湧き地点22箇所から12方向ずつプレイヤーを3秒歩かせて壁に
ぶつけ、正面へレイを飛ばして距離を測った（ブラウザ無しでNode上で再現、
`server/dom-stub.js` + `buildLevel` + `Player` + `WEAPONS` を直接使う形）。

結果: **「実際に壁へ止められた」ケース(22件)は、正面のレイが壁を
検出できた分について全て1.8m以内で拾えていた。** 射程外に見えた11件は
壁ではなく境界や段差で止まったノイズと判明。射程不足という仮説は
支持されなかった。

つまり幾何的な原因（射程・raycastの仕組みそのもの）ではなさそうで、
コード上も発火経路（`triggerEdge`→`_fire`→`onShot`→`_resolveShot`）に
見える範囲では問題が無かった。**次に本人が遊んで再現した時、
どの武器で・どの場所で・敵とすれ違った直後か等、状況を具体的に
教えてもらえると次の手が打ちやすい。**
