# 課題

手を付けていない物と、その時点で分かっている事実を置く。
数字は `npm run sounds` / `npm run check` で測った実測値。
着手する時に測り直すところから始めなくて済むように、根拠を一緒に残す。

---

## 足音が微妙（2026-08-04）

遊んでいて「シンプルに微妙すぎる」と言われた。測ったら理由がはっきり出た。

| 面 | 長さ | 打点 | 低音 | 重心 | 音量 |
|---|---|---|---|---|---|
| dirt | 691ms | 3 | 47.5% | 2504Hz | 0.32 |
| gravel | 608ms | 3 | 31.4% | 3494Hz | 0.26 |
| asphalt | 745ms | 1 | 53.0% | 2394Hz | 0.32 |
| concrete | 637ms | 2 | 49.2% | 2328Hz | 0.20 |
| metal | 687ms | 3 | 53.5% | 2615Hz | 0.35 |
| wood | 717ms | 1 | 41.4% | 1698Hz | 0.34 |

（参考: キル音 0.75-0.86 ／ 銃声 0.67 ／ 命中 0.47）

分かっている問題は4つ。

1. **1歩が長すぎる。608〜745msある。**
   歩く速さは毎秒2.5歩ほどなので、1歩の間隔は400ms。今の長さだと
   次の足音が始まっても前の足音がまだ鳴っていて、常に2〜3歩ぶんが
   重なった状態になる。踏むたびに輪郭が潰れて「サーッ」と混ざる。
   これが「微妙」の一番大きい部分だと思われる。目安は120〜200ms。

2. **1歩なのに打点が2〜3ある。**
   後から足した爪先の擦れの層と、残響の返りが別々の打点として立っている。
   1歩踏んだのに2回3回鳴っているように聞こえる。1歩は1打点にする。

3. **音量が0.20〜0.35で、他より一段小さい。**
   自分の足音は常に鳴っているので大きすぎても困るが、
   踏んだ感触が要る場面（走り出し・着地）では明らかに足りない。

4. **面ごとの差が出ていない。**
   asphaltとconcreteが重心2394Hz対2328Hzでほぼ同じ。
   metalも2615Hzで、金属らしさが数字に出ていない。
   材質を作り分けているつもりが、実際には音量とざらつきしか変わっていない。

着手する時の進め方: `tools/sound-lab.mjs` に足音は入れてあるので、
`npm run sounds` で `sounds/step-*.wav` を書き出して聴きながら、
上の4つを数字で追える。1歩の長さと打点の数は `tools/check-sound.mjs` に
下限を足して、二度と伸びないようにする。

---

## キル音（2026-08-04 一旦決着）

7回作り直して7回とも外した。こちらは音を聴けないので良し悪しの判定ができず、
遊ぶ側は判定できるが数字を触れない。この往復が終わらないのが原因だった。

一時期はロビーにつまみ（高さ・間隔・余韻・低音・芯・歪み・打点の数）を出して
遊ぶ側が直接回せるようにしたが、UIとして邪魔だったので畳んだ。
今は `src/core/audio.js` の `KILL_TUNE` 1つに固定してある。

作り直したくなったら、まずその値を触って `npm run sounds` で書き出して聴く。
低音の割合・重心・長さ・打点の数が数字で出るので、勘で直さずに済む。

---

## クラウドへ出した（2026-08-04 完了）

**https://blackout-fps.fly.dev** で動いている。Fly.io の東京(nrt)。

以前は同じWi-Fiの中だけで、部屋を立てた側は応答21ms、入る側は60msと、
サーバーが立てた人の端末にいるせいで構造的にホストが有利だった。
東京へ置いたことで両者が同じ距離になり、この不公平が消えた。

出す前に直した物が5つある。以下はその記録（同じ罠にまた掛からないため）。

1. **終了の合図(SIGTERM)を受け取っていない**
   `server/index.js` にハンドラが無い。再デプロイのたびに全試合が無警告で切断される。
   → 済。SIGTERM/SIGINTを受けたら理由(Sv.BYE)を全員へ配ってから閉じる
2. **サーバーのソースが全部読める**
   公開範囲が丸ごとなので `/server/sim.js`（当たり判定の実装）も `/package.json` も取れる。
   無認証で全部屋の合言葉と人数を返す `/rooms` も開いている。
   → 済。配る範囲を `index.html` / `src/` / three の一部に絞り、`/rooms` は閉じた。
     判定は `server/serve-rules.js` に切り出して、`tools/check-serve.mjs` が
     抜け道（符号化した .. など）込みで34項目試している
3. **サーバーを増やせない**
   部屋がプロセスのメモリ内（`server/room.js` の `rooms` Map）。同じ合言葉でも
   別の台に振られたら別試合になる。
   → `fly.toml` で台数を1に固定した。AWSやGCPの「勝手に台数を増やす」売りは
     使えないので、Lambda / Fargate+ALB / Cloud Run(既定設定) は不適合
4. **起動時に地形（三角形20万個）を組み終わるまでポートが開かない**
   `buildWorld()` が終わってから `listen` する。死活監視の猶予が短いと起動失敗と誤判定される。
   → 実測した。手元で2054ms、コンテナの中で2470ms。
     `/healthz` を足して、死活監視の待ち時間を20秒に設定した
5. **前にLANで遊んだブラウザが繋がらない**
   localStorage に `ws://192.168...` が残っていて、HTTPSのページからは混在コンテンツで弾かれる。
   → 済。httpsで開いた時だけ欄を畳み、「別の場所へ繋ぐ」で出せるようにした。
     手で打たれた時も wss:// へ寄せる

6つ目は出してから分かった。**512MBのマシンでは起動すらできなかった。**
地形を組むのにヒープ431MB（プロセス全体で586MB）要るので、
Nodeのヒープ上限が約256MBになる512MBでは届かず、
起動とOOM再起動を延々繰り返してポートを1つも開けなかった。
1GBへ上げて `NODE_OPTIONS=--max-old-space-size=768` を明示して解決。

**起動「時間」は測ったのに「メモリ」を測っていなかったのが見落としの原因。**

月$5〜6の見込み。地形のメモリを減らせれば512MB（月$3）へ戻せる。
他に検討した置き場所は AWS Lightsail 東京 / ConoHa VPS 東京 / Oracle Cloud 無料枠。

CDはGitHub Actionsから動かしている（`.github/workflows/deploy.yml`）。
鍵はGitHubのSecretsにあり、手元にもClaudeの手元にも無い。
mainへマージすると自動で出る。人が本番へ手でコマンドを打たない形。

## 地形のメモリが重い（2026-08-04）

地形(三角形198,344個)のOctreeを組むと、**ヒープを431MB食う**（プロセス全体で586MB）。
そのせいでクラウドのマシンを1GBにする必要があり、512MBでは起動すらできなかった。

原因はOctreeが三角形をTHREE.Triangle（中にVector3が3つ）のまま持っていること。
1三角形あたりオブジェクト4つ＋数値9個で、20万個ぶんが素直に効いてくる。

型付き配列（Float32Array）で座標を平坦に持てば大きく減らせるはずだが、
three側のOctreeをそのまま使えなくなるので、当たり判定を自前に置き換える話になる。
効果は大きい（マシン代が半分になる）が、当たり判定は一番壊してはいけない部分。

測り方: `node -e "import('./server/world.js').then(m=>{m.buildWorld();console.log(process.memoryUsage())})"`

---

## 手を付けていない物
- **1人用の敵が新しい要素を使わない**: 包帯も手榴弾も敵は使わない。
- **すり抜けられる物がある**: 胸の高さで測った1257点のうち3点が
  「見えているのに通れる」状態。「忘れていい」と言われたので放置している。
