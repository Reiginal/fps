# CLAUDE.md（このrepo専用）

BLACKOUT — ブラウザで遊ぶFPS。

全repo共通のルールは `~/.claude/CLAUDE.md`。ここには**このrepoだけの事情**を書く。

**このファイルはGitHubにも上げてある**（2026-08-06から。それまでは `CLAUDE.local.md` という名前で手元だけに置いていた）。
手元にしか無いと、GitHubのページから直したい時に何も分からないため。
鍵や個人情報は書かないこと（`LOG_KEY` は設定の仕方だけで、値は書かない）。

- **やりたいことの一覧** … `やりたいこと.md`
- **手を付けていない不具合** … `課題.md`（番号付き）

## 先に言っておくこと

**ここに書いてあるのは「今どうなっているか」であって、「こうでなければならない」ではない。**

今はThreeJSでWebGLを直接触っていて、テクスチャも効果音もコードで生成している。
これは今の実装がそうなっているだけで、決まりではない。
別の描き方・別のライブラリ・画像や音源を持つ形・そもそも別のジャンル、
どれも普通に検討していい。

やりたいことを言われた時に「今の作りだとできない」で返さない。
今の作りを変えれば済む話がほとんどなので、
**変える手間と得られる物を並べて、選べる形で出すこと。**

---

## 起動と検査

```bash
npm start          # ゲーム本体の配信と対戦の受け口。両方これ1本
npm run check      # ブラウザを開かずに走る検査。約12秒
npm run check -- -v  # 通った物の中身も全部出す（普段は落ちた物だけ出る）
npm run e2e        # 本物のブラウザで開く検査（1本）。サーバーは自分で起動して落とす
npm run lint       # ESLint
npm run sounds     # 音を sounds/*.wav に書き出して数字で並べる
```

**サーバーは勝手に起動しない。** PCが熱くなるので、起動はユーザー自身がやる。
確認が要る時は「起動して」と頼むか、サーバーを立てずに済む方法を探す
（例: `node -e "import('./server/world.js').then(m=>m.buildWorld())"` で地形だけ組む）。

**検査を足す時にやることは、`tools/check-なんとか.mjs` を置くことだけ。**
`tools/run-checks.mjs` が置き場を読んで見つけるので、
package.jsonにもci.ymlにも名前を書かない（前は3箇所を手で揃えていて、必ずずれた）。
本数と項目数も走らせた時に出るので、ここに数字を書かない。

---

## どこを見るか（直す前に、探す時間を減らすため）

```bash
node tools/map.mjs        # 全ファイルの一覧。何行あって、何を持っているか
node tools/map.mjs 発砲    # 「発砲」を含む区切りがどのファイルのどこにあるか
```

**地図は手で書いていない。** 各ファイルの冒頭コメントと、元から書いてある
`/* ------ 見出し */` の行を読んで並べているだけなので、古くならない。
（ファイルを足した時にやることも無い。冒頭コメントさえ書けば勝手に載る）

下の表だけは手書き。**症状の言葉からは機械で引けない**ので、ここに置く。

| 言われ方 | まず見る所 | 確かめる検査 |
|---|---|---|
| 弾が当たらない・当たり判定 | 対戦は `server/sim.js` の `resolveShot`、ソロは `src/main.js` の `_resolveShot`。**別物が2つある** | weapons, hitbox, rays |
| 撃ち味・反動・構え・持ち替え | `src/player/weapons.js`（「実装」より下） | weapons, loadout, reload |
| 動きが変・位置が引き戻される | `src/player/player.js`。**両側で同じ物が走る** | parity, doors |
| 滑り込み・しゃがみが変 | `src/player/player.js` の「スライディング」 | slide, parity |
| 体力・固さ・何発で倒れる | `src/net/protocol.js` の `HP`。**1人用と対戦で違う** | hp, heal |
| 弾痕・火花・煙・薬莢 | `src/world/effects.js` | — |
| 音 | `src/core/audio.js` | sound（`npm run sounds`で先に測る） |
| 画面の数字・撃破表示・順位表 | `src/ui/hud.js` | hud |
| 通れない・すり抜ける・地形 | `src/world/level.js` | doors, world, rays, worldgeo |
| 1人用の敵 | `src/ai/enemy.js` | swarm |
| 対戦のCPU（動かない・撃たない・強すぎる） | `server/bot.js` | bots |
| 席・ラウンド・勝敗・復帰 | `server/room.js` | lobby, modes, teams, rejoin |
| 遊び方の決まり | `server/modes.js`（**サーバーだけが持つ**） | modes |
| 送る項目を足す・食い違う | `src/net/protocol.js` を**両側同時に** | protocol |
| 他人がカクつく・見た目が変 | 見た目は `src/net/remote.js`、位置合わせは `src/net/client.js` | parity |
| 重い・熱い | `src/core/autoquality.js` と `src/core/framegate.js`。まず `/logs` のfpsを読む | framecap, autoquality, worldgeo |
| 設定を足す | `src/core/settings.js` の表（`src/ui/settings.js` は並べるだけ） | settings |
| 色・ボケ・輪郭・眩しさ | `src/core/postfx.js` | postfx |
| 開くと画面が真っ黒 | `import` の綴り | imports |

**ソロと対戦で持ち主が違う物がある。** 撃った判定と手榴弾は、
ソロが `src/main.js`、対戦が `server/` 側にそれぞれ別に居る。
片方だけ直すと「1人用では直っているのに対戦では直っていない」になる。
どちらの話かを先に確かめる。

判定は全部サーバーが持っていて、クライアントが送るのは入力と向きだけ。
理由は `src/net/protocol.js` の冒頭に書いてある。

## 本番

**https://blackoutfps.com** — Fly.io 東京(nrt)、512MB、常時起動、月$3。

**`https://blackout-fps.fly.dev` も生きている。** ドメインを取っても
Flyが元から配っているアドレスは消えないので、両方から同じ試合に入れる
（2026-08-11。DNSはCloudflare、`www`も同じ所を指している）。
遊ぶ人に送るのは`blackoutfps.com`の方。

**Cloudflareは間に立てていない（DNS onlyの灰色の雲）。** 立てると
`force_https = true`との噛み合わせと対戦の通信(WebSocket)を確かめる作業が要るので、
CDNを前に置くのは`やりたいこと.md`で別の項目にしてある。

```bash
flyctl status              # 動いているか
flyctl logs -a blackout-fps
```

mainへマージすると `.github/workflows/deploy.yml` が自動で出す。
鍵(FLY_API_TOKEN)はGitHubのSecretsにあり、手元には無い。
**人が本番へ手でコマンドを打たない形にしてある。**

### 出来事のログ（/logs）

入退場・試合の結果・画面のエラーを、サーバーの**記憶の中に最新500件**溜めてある。
`https://blackoutfps.com/logs?k=<鍵>` をブラウザで開くと表で読める。

- **デプロイすると全部消える。** ディスクを使っていないので。
  想定している使い方が「遊ぶ→不具合→その日に見る→直す→出す」なので、
  直した後に前のログが残っていない方がむしろ都合がよい（前後が混ざらない）
- 手元（`npm start`）では鍵なしで `http://localhost:8080/logs` が開く
- 本番で見るには鍵が1度だけ要る:
  `flyctl secrets set LOG_KEY=<好きな文字列> -a blackout-fps`
  設定するまで本番の `/logs` は404を返す（名前と環境が入る物なので、
  入れ忘れたまま全公開になるより見えない方がまし）
- **毎フレーム起きる物（位置・発砲・命中）は拾わない。**
  4人だと位置は毎秒240回・発砲は毎秒43回で、1日2GBと370MBになる。
  入退場や試合の結果は毎秒0.07回（1日0.6KB）。この間に線を引いてある。
  `tools/check-logs.mjs` の[10]がそこを見張る

## GitHubの運用

- `github.com/Reiginal/fps` — **public**
- **mainは保護されている。直接pushできない**（管理者でも弾かれる）。必ずブランチ→PR→マージ
- **CIが緑でないとマージできない。CIは30秒前後で終わる**（2026-08-07に2分30秒から削った。
  何を削ったかは `.github/workflows/ci.yml` のコメント）
- ブランチ運用は main のみ（GitHub Flow）。developは使わない
- **PR作成もマージも聞かずに進めてよい**（2026-08-08に変更。それまでは
  「PRを作ったらマージ可否を必ず聞く」だったが、
  「個人開発でスピード重視だから、問答無用でガンガンマージしていい」と言われた）。
  レビュアーがいないrepoなので、待つ相手がいない。CIが緑なら通す。
  **聞くのはマージの後、「次に何をやるか」だけ。** そこはAskUserQuestionで出す。
  PRとFile changesのURLは、聞くためではなく後から見るために本文へ出しておく
- **判断が要る場面では必ず聞く。** 止めないのは「もう決まっている作業を進める時」の話で、
  やり方が複数ある・仕様を決める必要がある・元の指示に無い物を足す時は、
  勝手に決めずにAskUserQuestionを出す。
  **基本は自律的に直し続けること**で、判断が要る所だけ手を止める
- **PRは細かく分けない**（2026-08-08追加。軽量化を5本に分けたら
  「意味ないなら分けるな」と言われた）。個人開発でレビュアーがいないので、
  分けて得られる物（レビューのしやすさ・巻き戻しの単位）の大半がこのrepoには無い。
  1つの作業の流れは1本のPRにまとめる。コミットは今まで通り意味の単位で分けてよい。
  これはこのrepo特有のルールで、仕事repoのPRの切り方には持ち出さない
- **draft→readyは踏まなくていい。** ci.ymlの引き金は `pull_request`（種類の指定なし＝既定）なので、
  PRを開いた時点でCIが走る。仕事repoの「readyにしてCIを起こす」作法はここでは効かない
- PRのテンプレート（設計判断・対応提案・チケット等）は仕事repo向けの物なので、ここでは要らない。
  概要・対応内容・動作確認・所感くらいで足りる

---

## このrepoで効いている考え方

### 画面と音は「測って」直す

このrepoの不具合は**ターミナルで見える層**と**画面/音を見ないと分からない層**にはっきり割れる。
後者を勘で直そうとして何度も外したので、測る道具を作ってある。

- `tools/check-*.mjs` … ブラウザ無しで走る検査。`server/dom-stub.js` の上で本物のクラスを動かす
- `tools/offline-audio.mjs` … WebAudioをNodeの中で実際に計算して波形を取り出す
- `tools/sound-measure.mjs` … 低音の割合・重心・長さ・打点の数を測る
- `tools/sound-lab.mjs` … wavに書き出す。`afplay sounds/kill.wav` で聴ける

**音が「ダサい」と言われたら、まず `npm run sounds` で測ること。** 勘で直さない。
過去に7回外している。「軽い」の正体は低音の割合が0.0〜0.4%だったこと（爆発は42%）。

### 軽さ（PCが熱くならないこと）は、機能より優先する

**遊ぶ人のPCが熱くなったら、その時点で他が全部どうでもよくなる。**
面白い機能を足しても、ファンが回り始めたら閉じられる。

だから何かを足す時は、毎回この順で見る。

1. **毎フレーム走る物を増やしていないか。** 増やすなら、それが本当に毎フレーム要るか
2. **描く物を増やしていないか。** メッシュ1つは描画1回ぶん。地面に置く物・演出・名札は全部これ
3. **配列の詰め直しをしていないか。** `shift()` や `splice(0,1)` は残り全部を1つずつ前へ動かす。
   毎フレームやると、長さぶんの仕事が毎フレーム乗る

実際に踏んだ形（2026-08-06、同じ日に4つ）:

- 描画の重さを測る仕掛けが、**2000件の配列を毎フレーム `shift()` していた。**
  測るための仕掛けが、測られる物を重くしていた
- **ミニマップを毎フレーム塗り直していた。** 168pxの枠でも、塗るたびに
  焼いた地図の切り出し＋拡大＋円と点の描画が走り、描き上がった絵を毎回画面へ送り直す。
  他人の位置が届くのは毎秒20回なので、それより速く塗っても点は1つも動かない
- HUDが**同じ値を毎フレーム書き込んでいた**（予備弾・走りの印）
- Tabを押している間、**順位表のHTMLを毎フレーム丸ごと作り直していた**

`tools/check-hud.mjs` の [軽さ] が、同じ値で何度呼んでも2度目から触らないことを見ている。

**数字で見られるようになっている。** 遊び終わりに fps の中央値と遅かった5%が
`/logs` へ1行残る（`tools/check-report.mjs` の [重さ]）。
「なんか重い」を感想で受け取らず、そこを読んでから触る。
**平均を見ない。** 60が続いて時々10まで落ちる端末は、平均だと57で「問題なし」に見える。

### 検査には3つの層があって、届く所も走る場所も違う

| | 見ている所 | 走る場所 |
|---|---|---|
| `npm run check` | 中身の理屈。Nodeの中でクラスを動かす | 手元・PRのたび・本番へ出す前 |
| そのうち `check-imports` | 読み込みの繋がり。importmapで行き先が決まるか | 同上（0.1秒） |
| `npm run e2e` | 本物のブラウザで開いてホームまで来るか | **本番へ出す前だけ**（2分かかる） |

**`import` を1行書き間違えると、他の検査は全部通ったまま、開くと真っ黒になる。**
実際に壊して確かめた（2026-08-06）。構文チェックも通る（読み込み先の解決までは見ないため）。
そこだけを `tools/check-imports.mjs` がブラウザ無しで拾う。
入口(index.html)から読み込みを全部辿って、行き先が決まるか・ファイルがあるか・
外へ配る決まりがあるかを見る。**残った差は「本当に画が出るか」だけ。**

E2Eは**1本だけにしてある。** 撃ち合いの中身を重ねて書くと、遅くて壊れやすい検査が増えるだけ。
本物のGPUが無い所ではこの場面をCPUで塗るので、**読み込むだけで1分近くかかる**。
そこにマウス操作を足すと、本体が詰まっていて合図の往復が返らず時間切れになる（実際なった）。
押す所はDOM側から呼んでいる。

**2分かかるので、PRのたびには走らせない**（2026-08-07）。
本番へ出す直前(`deploy.yml`)へ移してあり、落ちるとそこで止まって本番は前のまま。
移した先から消えても気づけないので、`tools/check-meta.mjs` の[5]が繋がりを見張る。

### 検査は「落ちること」を確認する

通ることより落ちることの確認が大事。実際に、
「ブランチ保護を設定したのに管理者は素通りできた」「つまみの開閉ボタンが何も畳めていなかった」
はどちらも**設定/実装しただけで確かめなかった**ために起きた。

### コメントは資産

長いコメントで「なぜそうなっているか」を残してある。ESLintに整形ルールを入れていないのは
自動整形がそれを崩すから。**コメントを消したり削ったりしない。**

---

## 次にやること

**`課題.md` に番号付きで書いてある。** 実測値も根拠もそちらに揃えてあるので、
ここには一覧を持たない（2箇所に書くと必ず片方が古くなる。実際に古くなった）。

**やりたいこと（機能の案と仕分け）は `やりたいこと.md`。**
どちらもGitHubに上げてあるので、手元が無くても読める。

「1から3まで消しておいて」と言えるように番号を振ってある。
消したら残りの番号を振り直すこと。

---

### MacとWindowsの両方を考える

**遊ぶ人はWindowsにもいる。手元がMacだからという理由で片方しか見ない、をやらない。**

2026-08-04に実際に踏んだ形: しゃがみをCtrlに割り当てていたので、
Windowsではしゃがみながら動くと `Ctrl+W`（タブを閉じる）、`Ctrl+R`（再読み込み）、
`Ctrl+D`（ブックマーク）、`Ctrl+F`（検索）、`Ctrl+1/2/3`（タブ切り替え）が全部発火して、
**遊べる状態ではなかった。** MacはこれがCommand+キーなので、こちらでは一度も踏まない。

キー・修飾キー・ファイルの区切り・改行・フォント・全画面まわりを触る時は、
**両方の環境で何が起きるかを書き出してから決める。**
片方でしか確かめられない時は、その事実をPRに書いて、遊ぶ人に確認を頼む。

### このrepoでは修正案を挟まない

**調査したら、そのまま直す。**「修正案を見せていいですか」「実装に進んでいいですか」の
2段階の承認は要らない（2026-08-05、「このrepoに関しては、修正案とか見せないでいいよ、
いきなりどうした」と言われた）。

全repo共通の`~/.claude/CLAUDE.md`にある2段階承認は仕事repo向けの物で、ここには持ち込まない。
不具合を言われたら、調べて・直して・測って・出す。
承認が要るのはPR操作から（それはCLAUDE.mdの「GitHubの運用」の通り）。

### 許可はこのrepoだけ広げてある

`.claude/settings.local.json` で Bash / Read / Edit / Write などを丸ごと許可してある
（2026-08-06、「このrepoに関しては割と広く許可だね、制限とっぱらっていいよ」と言われた）。
個人開発で、壊れて困る人が自分しかいないから。仕事repoは今まで通り都度確認のまま。

このファイルは `~/.config/git/ignore` の `**/.claude/settings.local.json` で除外されているので、
publicなrepoだが手元にしか残らない。

**それでも消えない制限が2つある。** どちらも `~/.claude/settings.json` の deny にあり、
deny は allow より強いので、ここで許可しても効かない:

- `git restore` … 変更を黙って捨てる道具。`git stash` を使う
- `.env` の読み書き … 鍵が会話に載る

この2つは「遅くなるから外したい」種類の物ではなく、**外すと戻せない事故が起きる**種類なので
そのままにしてある。外したくなったら、その時に相談すること。

### 並行して別のセッションが動いていたら、worktreeへ隔離してから作業する

このrepoは複数のClaudeセッションが同時に動くことがある（VS Codeの2窓・webのセッション等）。
**同じ作業ツリーに2セッションが同時に書くと事故る。** 2026-08-09に実際に起きた形:

- 相手の書きかけ（未コミットの数百行）が自分のgit statusに混ざる
- ブランチを切り替えると相手の書きかけごと引きずる
- 相手の調査用ファイル（*.tmp.mjs）をゴミと思って消してしまう（実際に消した）
- lintが相手の書きかけまで採点して赤くなる

**運用: 作業を始める時にgit statusへ自分の物でない変更が見えたら、
EnterWorktreeで自分をworktree（同じ書庫・別の机）へ隔離してから作業する。**
きれいなら今まで通りその場で作業してよい。
worktreeに入ったら、土台が古いことがあるので先に `git fetch` して
origin/main へ揃えること（fresh基準でも手元のfetchが古いと2〜3マージ遅れる）。

相手の書きかけには触らない。消さない・コミットに巻き込まない
（コミットはファイル名指定、必要ならhunk単位で自分の分だけ切り出す）。

**上に書いたことは、書いてあっても守られなかった。** 2026-08-12に、
書きかけの`index.html`が別セッションの`git add`に拾われ、
まったく関係のないコミットに混ざってPRになり、mainへマージされた。
書いた側は「自分の変更がgit statusから消えている」形で後から気づく。

**なので、相手が守るかどうかに賭けるのをやめて、`tools/guard-git.mjs`で止めている。**
Claudeの道具として走るgitだけを見張る（人が自分で打つgitには何も起きない）。

- `git add -A` `git add .` `git commit -a` `git stash`（パス無し）
  `git reset --hard` `git checkout .` `git clean -f` は**止まる**。
  相手が居るかどうかに関係なく止める（居るかを正しく数えるより、
  常に名指しで打つ方が確かなので）
- 名指しのコミットでも、**別のセッションがEdit/Writeで触ったファイルが混ざっていたら止まる。**
  誰がどれを触ったかは`.git/claude-claims.json`に溜めている（gitには入らない）。
  相手がコミットし終えると印は消えるので、そのファイルは普通に触れるようになる
- 枝を移る時（`git checkout -b`等）に相手の書きかけがあれば**聞く**。止めはしない
  （止めると、隔離するために枝を作ることすらできなくなる）
- **迷ったら通す側に倒してある。** 見張りが誤って止める方が作業は困る

繋ぎ方は`.claude/settings.local.json`のhooks。**このファイルはgitに入らない**ので、
新しい環境では繋ぎ直しが要る。外れていることに気づけるよう、
`tools/check-guard.mjs`の[5]が繋ぎの有無を見ている
（手元に設定が無いCIでは、そこだけ飛ばす）。

**設定を足した後に開いたセッションから効く。** 既に開いているセッションには届かないので、
入れ直した時は相手の窓も開き直すこと。

### スマホと繋がっているのはこのrepoだけ

Remote Controlを**繋いでいるのは全repoの中でここだけ**で、
仕事repoは個別に切ってある（詳細と、新しいrepoを作った時にやることは`~/.claude/CLAUDE.md`）。

**VS Codeを閉じるとセッションは死ぬ。** 出社中も動かしておきたい日は、
VS Codeを開いたままにしてMacを持って行かない。
Macを閉じて出る日は Claude Code on the web（クラウドで走る）の方を使う。

**`gh pr create` は共通設定で ask になっているが、ここでは確認が出ない。**
2026-08-06に実際に試して分かった。repo側の `.claude/settings.local.json` の allow が、
共通設定の ask より優先される（設定の近い方が勝つ）。
つまり**このrepoではPR作成もマージも止まらずに進む。**

**止まらないのは狙い通りで、実際そのまま進めてよい。**
個人開発でスピードを取るrepoなので、PR作成もマージも聞かずに通す
（2026-08-08に「問答無用でガンガンマージしていい」と言われた。
「GitHubの運用」の通り）。

**ただし止まらないのは「もう決まっている作業」に限る。**
やり方が複数ある・仕様を決める必要がある・言われていない物を足す時は、
権限があっても手を止めて聞く。権限は「待たされない」ためのもので、
「判断まで代わりにやる」ためのものではない。

## 引っかかりやすい所（今の作りの話。変えられないという意味ではない）

- **キル音は `src/core/audio.js` の `KILL_TUNE` 1つ。**
  ロビーに選択UIを出していた時期があるが、決まったので畳んだ。
  また迷い始めたら戻す価値はある
- **サーバーは1台でしか動かない。** 部屋がプロセスのメモリ内にあるので、
  今のまま台数を増やすと同じ合言葉でも別試合になる。
  増やしたいなら部屋の持ち方から変える話になる（できないわけではない）
- **検査の本数と項目数はもうどこにも書いていない。** `npm run check` が走った時に数えて出す
  （前は手で書いて、当然ずれた）
- **追跡から外したファイルは、マージしてmainへ戻った時に手元からも消える。**
  `git rm --cached` はそのブランチでは手元のファイルを残すので安心してしまうが、
  マージ後のmainには「そのファイルは無い」と記録されているため、
  切り替えた時にgitが作業ツリーからも消しにいく。gitignoreは追跡済みだった物を守らない。
  外す前に必ずコピーを取ること。
  （2026-08-04、課題.mdで実際に消した。`git show <消したコミット>~1:課題.md` で戻せた）
- **外部モデル（買った・もらったGLB）へ差し替える時は `src/player/glbview.js` を読んでから始める。**
  ライフルをCC0モデルへ差し替えた時（2026-08-06）に何度も踏んだ地雷が全部コメントに残っている。
  一番深かったのは「単体で測ると正しいのに、実戦(縮尺・回転が乗った状態)で測ると1.16倍になり
  画面の半分を覆う黒い板になった」で、**画面を撮って初めて気づいた**（スクショ機能はこれがきっかけ）。
  踏んだ地雷は全部 `tools/check-weapons.mjs` の `[モデル差し替え]` に回帰テストとして残っている。
  詳しい経緯と、**キャラの見た目に同じ手を使う時に武器より難しくなる理由**（キャラは
  `pivot()`で組んだ手作りの関節を毎フレーム直接回すアニメなので、剛体の武器と違って
  「箱に収めるだけ」では歩きもしゃがみもしない置物にしかならない）は issue #56 に整理してある
